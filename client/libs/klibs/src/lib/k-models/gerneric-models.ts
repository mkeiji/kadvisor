export interface GernericErr {
    message: string;
}

export interface LookupEntry {
    id: number;
    text: string;
    code: string;
}

export interface KSelectItem {
    value: any;
    displayValue: string;
}
